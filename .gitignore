bin/
obj/
Releases/
.idea/
*.DotSettings.user
.DS_Store

# Written by the csproj on first build; points at a local game install.
configkit/Properties/localSettings.props

# The reference shots are regenerated on demand, not authored - see
# docs/screenshots/README.md. The ck-* set is different: it is embedded in
# docs/STRUCTURED-CONFIG.md, so it has to be in the repo or the guide is broken.
docs/screenshots/settings-*.png
